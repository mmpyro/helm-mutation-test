apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}
data:
  {{- if and .Values.ingress.enabled (eq .Values.ingress.className "nginx") }}
  ingress: "nginx"
  {{- end }}
  static: "always"
